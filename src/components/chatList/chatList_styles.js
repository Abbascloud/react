import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles((ctx) => ({
  chat_container: {
    padding: "25px",
    width: "20%",
  },
  chat_item: {
    color: "#1976d2",
    paddingTop: "30px",
    cursor: "pointer",
  },
}));
