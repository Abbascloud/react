import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles((ctx) => ({
  wrapper: {
    height: "100px",
    position: "static !important",
    padding: "15px",
  },
  logo: {
    cursor: "pointer",
  },
}));
